export default function (cardValue) {
  const = five;
  if (cardValue === '5') {
    return five;
  }

  if (cardValue === '9') {
    return nine;
  }

  if (cardValue === '10') {
    return ten;
  }

  if (cardValue === 'jack') {
    return 10;
  }

  if (cardValue === 'queen') {
    return 10;
  }

  if (cardValue === 'king') {
    return 10;
  }

  if (cardValue === 'ace') {
    return 11;
  }
}
